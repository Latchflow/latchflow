triggers_list:
  get:
    summary: List trigger definitions
    tags: [Triggers]
    responses:
      '200':
        description: Triggers
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  type: array
                  items:
                    $ref: ../components/schemas/TriggerDefinition.yaml
              required: [items]

