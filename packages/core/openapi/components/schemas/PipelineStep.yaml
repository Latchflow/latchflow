type: object
properties:
  id: { type: string, format: uuid }
  actionId: { type: string, format: uuid }
  sortOrder: { type: integer }
  isEnabled: { type: boolean }
required: [id, actionId, sortOrder, isEnabled]
