type: object
properties:
  id: { type: string, format: uuid }
  kind:
    type: string
    enum: [TRIGGER, ACTION]
  key:
    type: string
  displayName:
    type: string
  jsonSchema:
    type: object
  isEnabled: { type: boolean }
required: [kind, key, displayName]
